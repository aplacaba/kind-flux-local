apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: forgejo
  namespace: forgejo
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`forgejo.localhost`)
      services:
        - name: forgejo-http
          port: 3000
