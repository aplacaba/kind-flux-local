# The Flux Operator is installed via raw YAML from the upstream release.
# See scripts/setup.sh for the install command.
#
#   kubectl apply --server-side -f https://raw.githubusercontent.com/\
#     controlplaneio-flux/flux-operator/main/releases/flux-operator.yaml
#
# This directory is kept as a placeholder; local manifests can override
# the upstream defaults if needed (via patches or a kustomize overlay).
resources: []
