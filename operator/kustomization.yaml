# The Flux Operator is installed via Helm on each cluster.
# See scripts/setup.sh for the install command:
#
#   helm upgrade --install flux-operator oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator \
#     --namespace flux-system --create-namespace --wait
#
# This directory is kept as a placeholder; local manifests can override
# the upstream defaults if needed (via patches or a kustomize overlay).
resources: []
